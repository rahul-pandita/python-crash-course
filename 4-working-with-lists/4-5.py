# SUMMING A MILLION
# Make a list of numbers from one to one million, and use min() and max() to make sure your list actully starts at one and ends at one million. Also, use the sum function to see how quickly python can add a million numbes

numbers = list(range(1, 1000001))

print(min(numbers))
print(max(numbers))
print(sum(numbers))